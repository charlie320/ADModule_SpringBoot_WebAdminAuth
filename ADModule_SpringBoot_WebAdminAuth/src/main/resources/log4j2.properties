name = PropertiesConfig
#Creates a directory in the project called "logs"
property.filename = logs
appenders = console, file

#Settings for the console log
appender.console.type = Console
appender.console.name = STDOUT
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = [%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n

#Settings for the file log
appender.file.type = File
appender.file.name = LOGFILE
#This line will direct log4j to save the file to "/logs/ProjectLogs.log"
appender.file.fileName = ${filename}/propertieslogs.log
appender.file.layout.type = PatternLayout
#Append the log file rather then overwrite
appender.file.append = true

loggers = file
#Package name
logger.file.name = com.perscholas.logging
#logger.file.level can be set to: trace, debug, info, warn, error, fatal, off
logger.file.level = trace
logger.file.appenderRefs = file
logger.file.appenderRef.file.ref = LOGFILE

#rootlogger.level can be set to: trace, debug, info, warn, error, fatal, off
rootLogger.level = trace
rootLogger.appenderRefs = stdout
rootLogger.appenderRef.stdout.ref = STDOUT